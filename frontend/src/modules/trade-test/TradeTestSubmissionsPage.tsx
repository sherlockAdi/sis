import TradeTestRecordsPage from "./TradeTestRecordsPage";

export default function TradeTestSubmissionsPage() {
  return (
    <TradeTestRecordsPage
      scope="admin"
      editable
      title="Trade Test Submissions"
      subtitle="Upload the trade video and certificate for each application, then approve passed candidates for deployment."
    />
  );
}
